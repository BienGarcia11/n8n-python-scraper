[build]
builder = "DOCKERFILE"
buildCommand = "docker build --build-arg BUILD_TIMESTAMP=$(date +%s) -t app ."

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60000

[env]
# Bulk Scraper Configuration
MAX_BULK_URLS = "10000"
EMBEDDING_MODEL = "text-embedding-3-small"
